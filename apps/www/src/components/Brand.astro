<a href="/" class="brand" aria-label="Beranda Ngerti.in">ngerti<span>.in</span></a>

<style>
  @layer components {
    .brand {
      font-family: var(--font-display);
      font-size: 32px;
      font-weight: 950;
      letter-spacing: -0.05em;
      color: var(--primary);
      line-height: 1;
    }
    .brand span {
      color: var(--foreground);
    }
  }
</style>
